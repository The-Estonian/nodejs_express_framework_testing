# Node.js express.js library/framework testing
### Getting familiar with options express.js has and  how it's better/worse then Django(python).

Used packages/modules in project:
* express
* body-parser
* ejs
